[pytest]
# Restrict test discovery to the top‑level `tests` package only.
testpaths = tests
python_files = test_*.py
asyncio_mode = auto
filterwarnings =
    ignore:pkg_resources is deprecated as an API:UserWarning:tradingview_scraper.symbols.technicals
